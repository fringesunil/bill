specVersion: "v1beta"
serviceId: "bill"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bill-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
